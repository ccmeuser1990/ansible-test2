---
- name: Install webserver
  hosts: all
  become: yes
  
  tasks:
    
    - name: Install httpd
      yum:
        name: httpd
        state: present
        
    - name: Start and Enable httpd
      service:  
        name: httpd
        state: started
        enabled: yes
        
